#ifndef CONFIG
#define CONFIG


// Either use all files in a directory
// or use only a single .pcd file (if commented)
#define PCD_DIR

// Use filters for the new data
#define NEW_DATA

#define VERBOSE 0

#endif //CONFIG
